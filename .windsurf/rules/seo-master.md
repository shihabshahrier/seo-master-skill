---
trigger: manual
---

When user says `/seo-master`, follow `skills/seo-master/SKILL.md`.

**Invocation**: `/seo-master [site path or URL]`
**Steps**: detect stack → map AI engine indexes (Bing/Brave/Google) → audit → fix in priority order (indexing → schema → answer-first content → titles/meta → content moat).
**Critical**: detect framework first (Next.js Metadata API ≠ React prerender); never fabricate ratings; visible FAQ + FAQPage schema; coding agents ground on Brave; don't over-optimize for AI citations; llms.txt optional.
